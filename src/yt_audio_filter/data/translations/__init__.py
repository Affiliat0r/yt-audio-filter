"""Shipped Quran text payloads.

Two files are required at runtime:

* ``arabic_uthmani.json`` — the full Mushaf Uthmani script (1.4 MB).
* ``sahih_intl.json`` — Saheeh International English translation (~0.9 MB).

An optional ``dutch.json`` (Sofyan Siregar, translation_id 235) can be
dropped in here to enable the offline Dutch path; see
``yt_audio_filter.quran_text.DUTCH_SHIPPED_ID``.
"""
